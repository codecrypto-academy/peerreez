declare module '@hyperledger/fabric-network' {
    // Minimal ambient declarations used only to satisfy TypeScript during development.
    // For production, install proper types or adjust to the official SDK typings.
    export type Contract = unknown;
    export type Network = unknown;
    export type Gateway = unknown;
    const _default: unknown;
    export default _default;
}
